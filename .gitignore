# ============================================================
# 用户数据 —— 绝不能提交
# ============================================================

# 通话录音与转写文稿，含客户隐私。一旦推到远程就收不回来了
results/

# 运行诊断日志
sherpa_diag.log

# ============================================================
# 可重新生成的产物 —— 提交它们只会让仓库变成几百 MB
# ============================================================

# 模型（约 229M）。首次启动会自动下载，或手动解压到 models/
models/

# PyInstaller 打包产物（约 252M）。运行 pyinstaller sherpa-asr.spec 可重建
_internal/
build/
dist/
*.exe

# VB-CABLE 虚拟声卡驱动安装包（第三方二进制，需自备）
VBCABLE_Driver_Pack45/

# ============================================================
# Python
# ============================================================
__pycache__/
*.py[cod]
.venv/
venv/
env/

# ============================================================
# 编辑器 / 系统
# ============================================================
.idea/
.vscode/
*.swp
Thumbs.db
desktop.ini
